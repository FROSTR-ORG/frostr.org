/**
 * Roadmap page (GitHub project board). Hidden from nav when ROADMAP_ENABLED is false.
 */
import { PageStub } from './PageStub';

export function RoadmapPage() {
  return <PageStub title="Roadmap" />;
}
